$(document).ready(function(){
	// var userName = prompt ("What's your name?");

	// if (userName){
	// 	$('#name').text(userName);
	// };

//Random number beetwen 19 and 120 needs to be generated by the system to start the game.
	var randomNum = Math.floor((Math.random() * (120 - 19 + 1) + 19));
	
	if(randomNum){
		$('#score').text(randomNum);
	}
//A random number beetwen 1 and 12 needs to be assigned to each crystal.
	var crystalRanNum;
	var userTotal = 0;
	var ruby = parseInt($(crystalRanNum).text());
	var diamond = parseInt($(crystalRanNum).text());
	var emeral = parseInt($(crystalRanNum).text());
	var topaz = parseInt($(crystalRanNum).text());

	$('.crystals').on('click', function(){
		console.log("you clicked me!")
	// 	if(ruby){
	// 		var crystalRanNum = Math.floor(Math.random() * 12) + 1;
	// 		userTotal += crystalRanNum;
	// 		// $('#userScore').text(crystalRanNum);
	// 		console.log(userTotal);
	// 	}
	// 	if(diamond){
	// 		var crystalRanNum = Math.floor(Math.random() * 12) + 1;
	// 		userTotal += crystalRanNum;
	// 		// $('#userScore').text(crystalRanNum);
	// 		console.log(userTotal);
	// 	}
	// 	if(emeral){
	// 		var crystalRanNum = Math.floor(Math.random() * 12) + 1;
	// 		userTotal += crystalRanNum;
	// 		// $('#userScore').text(crystalRanNum);	
	// 		console.log(userTotal);
	// 	}
	// 	if(topaz){
	// 		var crystalRanNum = Math.floor(Math.random() * 12) + 1;
	// 		userTotal += crystalRanNum;
	// 		// $('#userScore').text(crystalRanNum);
	// 		console.log(userTotal);
	// 	}

	}) 

	

//The numbers assigned to each crystal will change at each start of the game.
//A score keeper needs to be generated to match the user's score to the number generated
//if user's score === random number (WIN), else (LOSS).
//game should re-start without refreshing the page.


});